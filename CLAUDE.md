# チャンネル制作ルール(Channel Bible)

このファイルはチャンネル全体の憲法です。すべてのエピソード制作で必ず従うこと。
エピソードごとの指示は `episodes/<番号>-<名前>/brief.md` にある。

---

## 1. コンセプト

**"The hidden forces behind how the world works."**
地理・気候・歴史・経済・人口・教育などの「見えない力」が、国や文明の姿をどう決めてきたかを、データと地図で解き明かす英語の長尺解説チャンネル。

- 対象テーマは広くてよい。統一するのは**テイスト(問いの立て方・調査の深さ・映像の品格)**。
- 良い問いの例:
  - Why are most dictatorships in dry regions?
  - How did Genghis Khan cool the planet?
  - Why are hot countries poorer?
  - The world copied Finland's schools. Then Finland fell apart.
- 悪い問いの例:ただの雑学、ランキングの羅列、煽り、陰謀論、時事ニュースの要約。
- 一つの動画には**一つの中心的な問い**と、それに対する**一つの明確な答え(主張)**がある。

## 2. 対象視聴者

- 米国・英国・カナダ・オーストラリアの教養ある大人(25〜54歳中心)。
- The Economistを読む、ドキュメンタリーを好む、見出しを鵜呑みにしない人。
- 彼らは**賢くなった気分**と**「そう繋がっていたのか」という快感**を求めている。
- 見下す説明をしない。専門用語は使ってよいが、初出で一言で説明する。

## 3. 語り手と音声

- ナレーションは**合成音声(TTS)**。チャンネル全体で**一つの声に固定**し、声を「キャラクター」として育てる。**現在の声:Kokoro `bm_lewis`(低音の英国男性)**。
- 推奨エンジン:ElevenLabs(高品質・有料)、またはKokoro TTS(オープンソース・ローカル・無料)。どちらも使えるように、音声生成は差し替え可能なモジュールにする。
- 話速は**約165〜175語/分**(Kokoro speed 1.12)。文の間は0.16秒、段落の間は0.4秒。間で溜めるより、言葉を削ってテンポを作る。
- 語り手の国籍や個人的な背景には触れない。語り手は「冷静で少し皮肉の効いた、博識な案内人」。
- 字幕のタイミングは、音声から**強制アライメント(whisper系)**で単語単位のタイムスタンプを取って同期する。

## 4. 尺・構成・テンポ

- 長尺:**短く締まった構成を優先(目安8〜12分)**。尺を埋めるための言い換えや水増しはしない。
- ショート:長尺1本につき、最も意外なデータを使った縦型(1080×1920)を3本、各30〜55秒。
- **構成テンプレート**(毎回この骨組みを使う):
  1. **Cold open(0:00〜0:40)**:最も意外な事実か映像で始め、中心の問いを提示し、「この動画で答える」と約束する。チャンネル紹介や挨拶はしない。
  2. **Setup**:問いを理解するのに必要な最低限の背景。
  3. **Act 1〜3**:証拠を積み上げる。各Actの終わりに次への小さな問いを残す(open loop)。
  4. **Counterpoint**:反対の証拠や別の説明を正面から扱う。ここがチャンネルの信頼を作る。
  5. **Resolution**:中心の問いへの答えと、それが視聴者の世界の見方をどう変えるか。
  6. **Closing**:一文で締め、コメントを誘う問いを一つだけ出す。
- 画面は**3〜6秒に1回は何かが変化する**。

## 5. ビジュアル・アイデンティティ

**原則:画像生成AIは一切使わない。** 映像はすべて、コードで作るグラフ・地図・図解と、ライセンスの明確な実写・アーカイブ素材で構成する。

### 2つのモード
- **World mode(地図・地理)**:暗い、彩度を落とした衛星画像のベースマップ(NASA Blue Marble / Black Marble など、パブリックドメイン)。国や地域は半透明の色面で強調する。
- **Paper mode(データ・グラフ)**:クリーム色の紙の背景(#F4EFE3前後)、墨色の線と文字、方眼の薄いグリッド。
- 「世界を見るときは暗い地図、数字を見るときは紙」。このルールをチャンネルの署名にする。

### 色
- ベース:墨 #1B1B1B、紙 #F4EFE3、夜 #0E0F11。
- アクセント:**朱 #C8362D**(主役・悪化・危機)、**藍 #2B4C8C**(比較対象・安定)、**黄土 #C9A24A**(第三の要素)。
- 1シーンで使うアクセントは最大2色。国ごとの色はエピソード内で固定する。

### 文字(2026-09 改訂:制作者のフィードバックより)
- 書体は**2種類だけ**:主役の大きな数字は **DM Serif Display**、それ以外の文字はすべて **Archivo(800〜900の極太)**。斜体・大文字だけのラベル・枠付きのラベルは使わない。
- 文字は**太く大きく**:キャプション84px前後、主役の数字は350px以上、グラフのラベルは70px以上、目盛りは46px。
- 画面上の文字は**2か所まで**:①左上のキャプション枠(1行)、②中央の主役(数字またはグラフの端のラベル)。出典表示だけは左下に小さく例外として置く。
- **文字はどことも重ねない**(線・点・マーカー・他の文字)。ラベルは線の外側に配置する。
- **短いテロップを連続で出さない**。キャプションは1本あたり6秒以上表示する。
- 画面上のテキストは1枚最大12語。ナレーションを画面に書き写さない。

### 動き(Remotionで実装)
- **動きは多く**:カメラ(パン・ズーム)は常にゆっくり動かし、2〜3秒ごとに何かが起きる。ただし**場面を短く切り替えることで動きを作らない**(地図などは5秒以上見せ、その間を動きで持たせる)。
- **データやグラフは発表のように出さない**:グラフ全体を1.5秒前後で素早く描き、値は始点と終点だけ示す。その後は光る先端・矢印・カメラで動きをつける。
- **視線誘導**:常に注目点を1つにし、次の注目点へ動き(線・矢印・カメラ・移動する数字)で目を運ぶ。注目しない要素は薄くする。
- 主役の数値は**カウントアップ**する。
- 地図は常にゆっくりズーム・パン(Ken Burns)。静止した地図を出さない。
- 強調は色面の点灯、円のマーカー、細い矢印で行う。派手なエフェクトは使わない。
- トランジションは2種類に固定:ディゾルブ(0.5秒)と、紙がめくれる/暗転するモード切り替え。
- イージングはすべて ease-in-out 系で統一する。

### 出典表示
- データを使うすべてのシーンの左下に、小さく `Source: OECD PISA 2025` のように出典を表示する。

## 6. データのルール(最重要)

1. **一次情報のみ**:OECD、世界銀行、国連、IMF、各国統計局、査読論文、公的機関の報告書。ニュース記事や集計サイトは「手がかり」としてのみ使い、数字は必ず一次情報で確認する。
2. **すべての数字はデータファイルから**:画面に出す数字、ナレーションで言う数字は、すべて `research/data/*.csv` と `research/facts.md` に出典URL・表番号・取得日つきで記録してから使う。記憶や推測の数字は禁止。
3. **比較可能性に注意**:調査方法の変更、年による定義の違い、統計的に有意でない差を順位として誇張しない。必要なら画面かナレーションで一言断る。
4. **反対の証拠も見せる**:都合の良いデータだけを選ばない。Counterpointの章で必ず扱う。
5. **因果と相関を区別する**:「〜と関連している」と「〜が原因だ」を言い分ける。
6. **独自の分析を1つ入れる**:毎回、公開データを組み合わせるか加工して、他の解説動画にはない図を最低1つ作る。
7. 事実確認の結果を `research/factcheck.md` に残す(主張/出典/確認結果/備考)。

## 7. 台本の文体(AIっぽさを消す)

- 短い文と長い文を混ぜる。一文は平均15語前後。
- 具体的な数字・固有名詞・年を多用する。抽象的なまとめ文で段落を終えない。
- 2分に1回程度、乾いたユーモアか皮肉を一つ入れる(誰かを貶めない範囲で)。
- 視聴者の予想を先回りして裏切る:"You'd think X. The data says otherwise."
- **テンポ最優先**:一つの事実は一度だけ言う。言い換え・前置き・余韻の一文を削る(「まだこの話?」と思わせない)。一つのデータの話は長くて20〜25秒。
- **データは毎年の値を読み上げない**。傾向と結論をスマートに言う(例:「2006年以降、一度も上がっていない」)。
- **禁止表現**:delve, tapestry, testament to, in today's world, it's important to note, navigate the complexities, a rich history, let's dive in, buckle up, in conclusion, at the end of the day, game-changer, unlock, embark, realm, landscape(比喩として), "Not only X, but also Y" の多用、三つ並べの修辞の多用、各段落末の要約文。
- 結論を濁さない。データを踏まえて主張を述べ、不確かな部分は不確かだと言う。
- 政治的に敏感なテーマでは、特定の国や集団を嘲笑しない。事実と解釈を分ける。

## 8. 音楽・効果音

- BGMはライセンスが明確なもの。**Episode 01 は Pixabay「Cinematic Documentary」(Lexin_Music)に決定**。コード生成曲(`tools/make_music.py`)は「寂しい」との評価のため非推奨。参考:旧方針はコード生成のオリジナル曲(調査もの向け:短調・92BPM・低音パルス・時計の刻み・まばらなピアノ)。ナレーションより常に-18dB以上小さく、話している間は自動で約4dB下げる。制作者が後から差し替えられるよう、BGMなし版も書き出す。
- 効果音は最小限(地図の点灯、数値確定時の小さなクリック程度)。
- 使用した素材はすべて `credits.csv`(素材名/作者/ライセンス/URL)に記録する。

## 9. サムネイルとタイトル

- サムネイル:1280×720。**一つの強い図(地図かグラフ)+最大5語**。セリフ体。アクセントは1〜2色。人物写真や驚いた顔は使わない。Remotionの `<Still>` で生成し、毎回3案を作る。
- タイトル:問いか、意外な主張。60字以内。煽るが嘘はつかない(動画の中身で必ず回収する)。
- 概要欄:2〜3文の要約、チャプター、出典一覧(全URL)、素材クレジット。

## 10. 技術スタックとフォルダ構成

- 映像:**Remotion**(React)。共通コンポーネントは `engine/` に置き、全エピソードで再利用する。
  - 使用前にRemotionのライセンス条件(個人・小規模チームは無料)を確認すること。
- データ処理:Python(pandas)。地図:Natural Earth(パブリックドメイン)+ d3-geo。
- 音声:TTSモジュール(差し替え可能)+ whisper系の強制アライメント。
- 出力:1920×1080、30fps、H.264。字幕は `.srt` で別途出力する。

```
/
├── CLAUDE.md                  ← このファイル
├── engine/                    ← Remotion共通部品(全エピソード共通)
│   ├── components/            LineChart, BarRank, WorldMap, RegionHighlight,
│   │                          StatCallout, SourceTag, TitleCard, QuoteCard, ...
│   ├── theme.ts               色・フォント・イージングの定義
│   └── tts/                   音声生成・アライメント
└── episodes/
    └── 01-finland/
        ├── brief.md           ← エピソード指示
        ├── research/          facts.md, factcheck.md, sources.md, data/*.csv
        ├── script/            script_en.md, script_ja.md(確認用の日本語訳)
        ├── storyboard.md      シーン表
        ├── audio/
        ├── remotion/          このエピソード固有のシーン
        ├── credits.csv
        └── out/               完成動画、字幕、サムネイル、概要欄
```

## 11. 作業の進め方

- 各エピソードは段階ごとに進め、**指定されたチェックポイントでは必ず作業を止めて、人間の承認を待つ**。
- 承認前に、全編のレンダリングや有料APIの大量呼び出しをしない。
- 迷ったら、見た目の派手さより**正確さと明快さ**を優先する。

## 12. 公開前チェックリスト

- [ ] すべての数字が `facts.md` にあり、一次情報で確認済み
- [ ] Counterpointの章がある
- [ ] 禁止表現が台本に残っていない
- [ ] すべてのデータシーンに出典表示がある
- [ ] 画像生成AIの素材が一つもない
- [ ] 静止したままのシーンが6秒を超えていない
- [ ] 音声と字幕がずれていない
- [ ] BGMがナレーションを邪魔していない
- [ ] `credits.csv` が完成している
- [ ] サムネイル3案、タイトル3案、概要欄(チャプター・出典つき)がある
- [ ] YouTubeの合成コンテンツ開示ルールを確認した
